fn main(): i32
{
    let a: i32 = 123 - 23;
    
    noop();
    noop2(1,2,3);

    let b: i32 = 456 + sum(a, a);
    {
        let c: i32 = 8 * 1000;
        b = c;
    }
    let result: i32 = sum(a, b);
    
    return result;
}

fn noop()
{
}

fn noop2(a: i32, b: i32, c: i32)
{
    return;
}

fn sum(a: i32, b: i32): i32
{
    return a + b;
}
